import { FoundationPlaceholderScreen } from '@/components/foundation-placeholder-screen';

export default function EnrollmentReviewScreen() {
  return (
    <FoundationPlaceholderScreen
      icon="clipboard-outline"
      title="Enrollment review"
      description="Enrollment review is intentionally inactive until camera, model, and liveness work begins."
    />
  );
}
