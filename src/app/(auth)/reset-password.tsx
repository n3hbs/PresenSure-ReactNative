import { FoundationPlaceholderScreen } from '@/components/foundation-placeholder-screen';

export default function ResetPasswordScreen() {
  return (
    <FoundationPlaceholderScreen
      icon="lock-closed-outline"
      title="Reset password"
      description="Password reset is intentionally a placeholder until the backend reset contract is finalized."
    />
  );
}
