import { FoundationPlaceholderScreen } from '@/components/foundation-placeholder-screen';

export default function ForgotPasswordScreen() {
  return (
    <FoundationPlaceholderScreen
      icon="key-outline"
      title="Forgot password"
      description="Password recovery route is reserved for the authentication foundation. The Laravel reset workflow can be connected in the next auth pass."
    />
  );
}
