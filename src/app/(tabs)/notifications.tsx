import { FoundationPlaceholderScreen } from '@/components/foundation-placeholder-screen';

export default function NotificationsScreen() {
  return (
    <FoundationPlaceholderScreen
      icon="notifications-outline"
      title="Notifications"
      description="Notification routing is prepared. Push-token registration and permission prompts will be connected when notifications are implemented."
    />
  );
}
