import { FoundationPlaceholderScreen } from '@/components/foundation-placeholder-screen';

export default function HistoryScreen() {
  return (
    <FoundationPlaceholderScreen
      icon="calendar-outline"
      title="Attendance history"
      description="History will connect to the Laravel API in a student read-feature phase. No attendance records are created here."
    />
  );
}
